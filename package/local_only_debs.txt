# .deb filenames (not package names — matches debcache/'s own naming)
# that scripts/build_deb_cache.py includes directly from debcache/
# without ever attempting real apt resolution for them at all — for
# packages that are genuinely dead upstream (no configured repo's Packages
# index carries them anymore, confirmed via a real "E: Unable to locate
# package" from apt itself, not assumed) but that this project already has
# a real, previously-recovered .deb for.
#
# This is NOT the same situation as com.ih8sn0w-squiffy-winocm.p0sixspwn
# (which DOES have a real Packages stanza — apt finds it fine — it just
# genuinely doesn't satisfy its own Depends: firmware constraint against
# this project's synthetic firmware version, which is correct, expected
# behavior for a firmware-version-gated package, not a dead-upstream one).
# That distinction is why this is a separate list from the ordinary
# packages.txt resolution flow, rather than one more tolerated warning.
#
# Entries here are still checked against Blackb0x/Misc/prebake_package_blacklist.txt
# like anything else in picklist.txt — see that file for why "essential"
# is listed there too (real, unmet Depends: on cydia, which is
# deliberately never bake-time-preinstalled).
essential_0-1_iphoneos-arm.deb

# net.tihmstar.etasonuntether is deliberately NOT here.
#
# It is applied statically instead, by BakeRamdisk.cpp's stageEtasonatv():
# extracted straight from its real .deb in debcache/, with this project's
# own untether.bin substituted for the one that .deb ships (see that
# function's own comment and Blackb0x/Misc/README.md for why the override
# exists), then registered with a real held dpkg status stanza. Shipping it
# through this local repo as well would mean two install paths for one
# package, with the static override being the one that actually matters --
# nothing the repo copy could add, and a second source of truth to keep in
# sync.
#
# It remains in package/packages.txt and is a tolerated resolution failure in
# build_deb_cache.py's KNOWN_EXPECTED_UNRESOLVABLE, exactly like its sibling
# com.ih8sn0w-squiffy-winocm.p0sixspwn: both are firmware-gated persistence
# payloads (`Depends: firmware (= 8.4.1)` here) that cannot satisfy the
# sandbox's synthetic firmware pin, and both are applied statically rather
# than by apt.
