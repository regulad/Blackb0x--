# misc

Now that `Blackb0x/ramdisk/` has been deleted outright (see
`.claude/NEO_FLOW.md`), this directory is where
everything from it worth keeping for provenance/reference lives instead.
No original source tarball is needed going forward anymore — see
"Original tarballs" below: every one, including `tihmstar-untether.tar`
(now superseded by the real `net.tihmstar.etasonuntether-1.3.1.deb` in
`debcache/` — see "etasonATV / tihmstar-untether provenance" below)
and `RamdiskBins.tar` (unpacked into `bin/`), has been dropped — with one
exception: `untether.bin` itself, kept standalone (see below), since it's
an open forensic question in its own right, not just install-time payload.

- `rc.boot` — **deleted**. This was the restore ramdisk's early-boot
  script (behavior recovered and written up in this entry), kept here on the
  assumption `bakeRamdisk()` would need to re-splice it. That assumption
  didn't hold: the entrypoint splice target ended up being `/sbin/launchd`
  (see docs/HISTORY.md's "Entrypoint injection point" entry), never
  `/etc/rc.boot` at all in the current design, so there was nothing left
  for this file to feed. Recovered its actual content once (from
  `Blackb0x/Files/ssh.tar` in commit `85e2d8b`, before deleting it for
  real) to check for logic the current entrypoint might be missing — it
  turned out to be `msftguy/ssh-rd`'s own rc.boot, not something written
  for this project at all: remounting the ramdisk rw, freeing RAM by
  deleting bundled firmware update blobs, disabling `/sbin/reboot` so its
  rescue shell can't be killed, starting `sshd` on the ramdisk itself (the
  same early-SSH-bootstrap approach already investigated and rejected
  elsewhere in this file), and finally chaining into Apple's own real
  `restored_external`/`restored_update`/`restored`/`ramrod` — genuine
  NAND-reflashing daemons, which this project must *not* invoke (a
  jailbreak persists files onto the already-installed OS; it doesn't
  reflash it). The one real gap found: `nvram auto-boot=1`, which
  SecureROM/iBoot apparently clears once a real DFU payload has run — now
  ported into `entrypoint.c`'s own `set_auto_boot()`, called right before
  every `reboot(2)`.
- No `sshtar/` here anymore. `msftguy/ssh-rd` (the `third_party/ssh-rd`
  submodule, symlinked in as `Misc/sshtar` for one session) was
  investigated as the real, confirmed source of the raw SSH-bootstrap set
  the original disassembled binary staged (`bash`, `ls`, `sh`, `sshd`,
  `scp`, `sftp-server`, `mount.sh`, `mktar.sh`, `device_infos`, 4 of 5
  needed dylibs) — a byte-for-byte match, verified by downloading and
  checking a couple of these directly. But that whole set turned out to be
  `ssh-rd`'s own "Automated SSH ramdisk creator/loader" tool: a *rescue SSH
  shell that runs during the ramdisk-boot stage itself* (its own
  `mount.sh` does exactly what `entrypoint.c` already does directly via
  raw `mount()` syscalls), entirely separate in purpose and timing from
  the real `openssh` package `postinstall.sh` installs onto the fully-booted
  device afterward — and `entrypoint.c` never spawns/execs anything to
  begin with, so staging these binaries alone would never have started a
  session even if wired in. NEO_FLOW doesn't need a ramdisk-stage rescue
  shell, so `third_party/ssh-rd` was removed entirely (submodule
  deregistered, `.gitmodules` entry gone) rather than left unused.

## `picklist.txt` (not checked in here — see below)

Machine-generated by `scripts/build_deb_cache.py`, but **not** written
anywhere in this repo, including this directory — it and its
`Packages`/`Packages.gz` companion are pure build-time artifacts now,
generated into a temp directory that `bakeRamdisk()`
(`src/BakeRamdisk.cpp`, the actual "ramdisk build program")
owns and shells out to `build_deb_cache.py --output-dir <tempdir>` to
populate, every time a ramdisk gets baked. Documented here anyway since
this is where its provenance/behavior is explained. It's the resolved
output of running `package/packages.txt` (a flat package-name list)
through real `apt` dependency resolution against this project's own
`*.list` sources: the exact, deduplicated set of `.deb` filenames under
`debcache/` that actually get copied into the ramdisk overlay's
`files/debs/` — not the whole (append-only, never-pruned) `debcache/`
directory, which accumulates every version ever fetched.

Running it against the current `packages.txt` resolves all but one name
through live apt attempts: `com.ih8sn0w-squiffy-winocm.p0sixspwn`
genuinely can't resolve at all — its `Depends: firmware (>= 6.1.3),
firmware (< 7.0)` only matches a 6.1.4 device, and this project's
sandboxed resolution declares its synthetic "firmware" version as `8.4.2`
(see the script's own comment for why: it can't satisfy every firmware
range this ecosystem has ever used in one resolution pass, so it picks
this project's newest/most-capable supported target). This isn't a bug —
per `.claude/NEO_FLOW.md`, per-branch persistence payloads like this are
meant to be installed as direct loose-file copies (extracted straight out
of their real `.deb` — see `BakeRamdisk.cpp`'s `stageP0sixspwn()`), not
resolved through apt at all, so dropping out of this list here is the
correct signal, not a gap to fix.

Two more names never even attempt live apt resolution at all —
`package/local_only_debs.txt`'s own mechanism, a real local
`file://` repo built from `.deb`s this project already has:
`essential`, genuinely not found in any of the five configured repos
today despite `debcache/essential_0-1_iphoneos-arm.deb` already
existing here from some earlier, no-longer-reachable source; and
`net.tihmstar.etasonuntether`, whose real Packages stanza on
`repo.tihmstar.net` *does* exist (it would genuinely resolve there,
firmware-gate aside) but which moved to this same local mechanism once
that repo's live index turned out to be an unreliable dependency (see
`misc/apt/net.tihmstar.list.disabled`) — same end state as
`essential` (a local file:// repo, not a live one), different reason for
getting there.

`bigboss`, `modmyifone`, `saurik`, `zodttd` used to be in `packages.txt`
too, and used to show up in every single resolution run as "no
installation candidate" warnings — confirmed, directly against a real
bake run, to be permanently dead: these are sources.list.d *basenames*
that leaked into `packages.txt` as if they were real package names during
the original dependency-graph scrape (see `docs/HISTORY.md`), not
temporarily-unavailable packages. Removed outright rather than left to
warn on every future build forever.

## Original tarballs — none kept anymore, all dropped (bar one loose file)

`ssh.tar`, `p0sixspwn.tgz`, `tihmstar-untether.tar`, `Debs.tar`,
`ATV-Cydia.tgz`, `RamdiskBins.tar` — each one's only still-relevant content
is already preserved elsewhere, individually, below or in `debcache/`:

- `ssh.tar` — its only known content was static SSH host keys, already
  deleted for real (not just here) as a security fix: shipping identical
  host keys in a public repo let anyone impersonate a jailbroken device, so
  `patchRamdisk()`/`BakeRamdisk.cpp` generates a fresh RSA host key per bake
  instead (see `docs/HISTORY.md`).
- `p0sixspwn.tgz` — its persistence payload is superseded by the real
  `com.ih8sn0w-squiffy-winocm.p0sixspwn_1.4-1_iphoneos-arm.deb` in
  `debcache/`.
- `tihmstar-untether.tar` — tihmstar's EtasonATV untether payload (see
  "etasonATV / tihmstar-untether provenance" below), long believed to have
  no independently re-downloadable source. Superseded once the real,
  long-lost `net.tihmstar.etasonuntether-1.3.1.deb` itself turned up (now
  in `debcache/`) — `BakeRamdisk.cpp`'s `stageEtasonatv()` extracts the
  same four files straight out of that `.deb` instead of this tarball now.
  One file from it — `untether/untether.bin` — is kept as a standalone copy
  at `misc/untether.bin` even though nothing installs it anymore:
  it's not a byte-for-byte match of either of tihmstar's own two published
  `untether.bin` builds (`etasonuntether-1.3.1`'s or
  `untetherhomedepot`'s — see "etasonATV / tihmstar-untether provenance"
  below), and where this specific 43,680-byte compiled build actually came
  from is still an open question (`.claude/TODO.md` item 1) — worth having
  the real bytes on hand for, independent of the now-retired tarball.
  `orig_untether.bin` (its unpatched 2021-04-17 predecessor, differing in
  15 branch-immediate bytes) is not duplicated here — recoverable from this
  same tarball via git history (`git show <pre-removal commit>:Blackb0x/Files/tihmstar-untether.tar`) if that comparison needs re-running.
- `Debs.tar` — originally seeded `debcache/`'s `.deb` set; that
  directory is the real, current source of truth now, not the tarball.
- `ATV-Cydia.tgz` — its only content still in use by anything (4 files) is
  extracted individually below; the rest of this ~15MB tarball was a flat,
  pre-extracted, unattributed Cydia filesystem tree already superseded by
  the real `.deb`-driven install NEO_FLOW.md describes.
- `RamdiskBins.tar` — the ramdisk-stage tool set, unpacked flat into `bin/`
  (see below) rather than kept packed; nothing gained by leaving it as a
  tarball once it's this small and this static.

## `bin/` — gone entirely now (was: the ramdisk-stage tool set, unpacked from `RamdiskBins.tar`)

Originally `dirhelper`, `gzip`, `libiconv.2.dylib`, `plutil`, `sleepcmd`,
`tar`, `uname`, `untar` — flat, matching this directory's existing
convention, dropping the original tarball's `usr/bin/`/`usr/lib/`
structure (the AppleDouble `._*` resource-fork sidecar files tar carried
alongside several of these were also dropped — pure macOS metadata, never
used on device). `dirhelper` no longer lives here — moved up to
`Misc/dirhelper` directly once its real nature turned up (see that
section below); it was only ever miscategorized as part of "the
ramdisk-stage tool set" by virtue of sharing a tarball with the genuine
utilities, not because it is one.

`gzip`, `tar`, `uname`, and `sleepcmd` were later deleted outright (not
just left unreferenced) once nothing staged them into the ramdisk at
all — every real Cydia package the jailbreak actually installs already
covers the same functionality, confirmed directly against each vendored
`.deb`'s own payload, not assumed: `gzip_1.6-7_iphoneos-arm.deb` ships
`./bin/gzip`, `tar_1.29-10_iphoneos-arm.deb` ships `./bin/tar` and
`./usr/bin/tar` (and `tar` is a direct `packages.txt` entry, not merely
transitive), and `coreutils-bin_8.12-9_iphoneos-arm.deb` ships both
`./bin/uname` and `./bin/sleep` — the latter functionally identical to
`sleepcmd` (confirmed via `strings`/disassembly: `sleepcmd` is a trivial
wrapper around libSystem's own `sleep()`, and is armv7/arm64-only besides,
so it couldn't even run on the armv6-only AppleTV2,1). `untar` was deleted
too, for a different reason — see its own paragraph below.

`plutil` was deleted later still, once a real source turned up after all:
this file's own earlier claim that "no package in any of this project's
three configured repos ships it" was true as far as it went, but nobody
had checked BigBoss's `com.ericasadun.utilities` specifically — a real,
still-live, dependency-free package (no Depends:, no postinst/preinst at
all) that ships `./usr/bin/plutil`, confirmed MD5-identical byte-for-byte
to this checked-in copy (`16dc1bc39e4d3f8c6bf9f9f537722587`) against the
live repo's own index, not assumed. Added to `packages.txt` directly.

`libiconv.2.dylib` was deleted last, and `bin/` along with it (nothing
left once it was gone) — not because a package covers it like the others
above, but because it never needed a package in the first place. Its own
embedded SCCS "what" string gives it away directly:
`@(#)PROGRAM:iconv.2  PROJECT:libiconv-35` — Apple's own real
`opensource.apple.com` project-naming convention, not a jailbreak-
community binary at all. Confirmed by downloading the actual source:
`https://opensource.apple.com/tarballs/libiconv/libiconv-35.tar.gz`
(HTTP 200, a real Xcode-project-wrapped build of upstream GNU libiconv —
its own `libiconv.plist` build manifest reports `OpenSourceVersion: 1.11`,
a real GNU libiconv release), also mirrored officially on GitHub at
`apple-oss-distributions/libiconv`. `/usr/lib/libiconv.2.dylib` is a core
Darwin system library Apple bakes into the base OS on every iOS/tvOS
device at the factory, the same as `libSystem.B.dylib` — unlike
`gzip`/`tar`/`uname`/`sleepcmd`/`plutil` above (real gaps in a stock
retail OS that a Cydia package then filled), this one was never actually
missing from a real device in the first place, so there was never
anything for a package to provide.

**`ldid` and `otool` deliberately excluded**: the real `ldid` .deb
(`debcache/ldid_1.2.1_iphoneos-arm.deb`, already in
`packages.txt`) provides `ldid`, and saurik's `odcctools` package
(confirmed by extracting `odcctools_286-8_iphoneos-arm.deb` directly from
`apt.saurik.com` — real `./usr/bin/otool` inside) provides `otool` — added
to `packages.txt`, not yet fetched into `debcache/` (that happens
automatically, the next time `scripts/build_deb_cache.py` runs, the same as
any other name added there). Nothing needs a raw copy of either before
`postinstall.sh` gets a chance to install the real packages — see
`.claude/NEO_FLOW.md`. `plutil` itself is gone now — see
the `bin/` section above for `com.ericasadun.utilities`, the real BigBoss
package that turned out to ship it (confirmed only by checking that repo
specifically; the earlier "no package in any of this project's three
configured repos ships it" claim, from downloading and inspecting all
~550 of saurik/awkwardtv/this fork's own packages directly, was accurate
as far as it checked — BigBoss just wasn't one of the three at the time).

`untar` was deleted too, on different grounds than the four above: nothing
in this codebase — not `entrypoint.c`, not `postinstall.sh`, not any
script — ever referenced it by name, and unlike `gzip`/`tar`/`uname`/
`sleepcmd`, no vendored package ships anything by that name either, so
there was no real replacement to point to — it was simply genuine dead
weight. Real `tar` (the real package now installed by the jailbreak)
already handles extraction itself (`tar -x`), which is normally all
"untarring" needs; a *separate* `untar` binary alongside it suggests it
was either a narrower-purpose tool (e.g. hardcoded to a fixed extraction
root, no flags) or leftover cruft from whatever originally assembled
`RamdiskBins.tar` — never conclusively determined which, but moot either
way once nothing referenced it.

## `dirhelper` — not a utility, promoted out of `bin/`

Lives at `Misc/dirhelper` directly now, not `Misc/bin/dirhelper` — it was
only ever sitting in `bin/` by accident of packaging (bundled inside the
same `RamdiskBins.tar` as the real command-line utilities above), not
because it is one. It's the file `entrypoint.c`'s `install_ios7_dirhelper()`
reads and its only consumer swaps into `/usr/libexec/dirhelper` as the
iOS7/8.x-(non-8.4) persistence exploit itself — real Apple/Cydia code
never touches that path at all. Don't confuse it with `p0sixspwn`'s own,
entirely separate `dirhelper` payload
(`debcache/com.ih8sn0w-squiffy-winocm.p0sixspwn`'s own
`usr/libexec/dirhelper`, installed via a different call for the 6.1.4
branch — confirmed genuinely different files, not the same binary shared
by coincidence: a real 50KB Mach-O vs. p0sixspwn's own 262-byte bash
script, different hashes).

**What it actually does** (reconstructed directly from its own strings and
symbol table — `main`/`remount`/`tar`/`run`/`file_exists` — no source was
ever found, see below): remounts `/` via `/sbin/mount / rw,suid,dev`,
self-tests by creating and deleting a throwaway `/rwtest` file, checks
`/usr/share/terminfo/x/xterm-xi` as a proxy marker for "is Cydia already
installed", and if not, extracts a bundled `/private/var/Cydia.tar` using
its own accompanying `/private/var/tar` binary (`posix_spawn`,
`--no-overwrite-dir`) — a self-contained Cydia bootstrap independent of
any real package manager. It finishes by running every script under
`/etc/rc.d/` (a generic post-boot hook). Watermarked "iH8sn0w is here
dawg!" in its own string table. Since real `/usr/libexec/dirhelper` is a
legitimate daemon the OS invokes automatically and repeatedly with root
privilege, replacing it makes iOS/tvOS unknowingly re-run this whole flow
— the actual persistence mechanism for a firmware generation with no
other untether (the legacy tether flow: "genuinely tethered, no
untether exists for this range at all").

**Where it came from: the ether.** Extensively searched and could not
find this binary's source, or even a second copy of it, anywhere:

- Confirmed present, byte-identical (`3fa84e1019032ac26829228cf05cd0ee815ffb2d`
  git blob hash), in the original upstream `NSSpiral/Blackb0x` repo at
  `Blackb0x/Files/dirhelper` since its very first commit (`85e2d8b`,
  "Initial Commit" — no message, no context). A real user's debug log
  on that repo's issue tracker (#68) shows it being extracted at runtime
  from `Contents/Resources/RamdiskBins.tar` inside the original macOS
  `Blackb0x.app`, alongside `libiconv.2.dylib`/`plutil`/`untar`/`uname`/
  `gzip`/`otool`/`ldid`/`sleepcmd`/`tar` — a flat grab-bag of ARM
  binaries the original developer(s) assembled themselves, with zero
  provenance recorded for any individual file in it.
- Zero results, for every one of five distinctive strings from this
  binary, across GitHub's entire code-search index (`"RootFS Successfully
  remounted as READ WRITE"`, `"iH8sn0w is here dawg"`, `"Cydia is still
  installing"`, `"Cydia should now be installed"`, `"Why does this rwTest
  exist"`) — the source was never checked into any public repo GitHub can
  see.
- Not present in any of iH8sn0w's own 26 public repos (`github.com/iH8sn0w`),
  including `iBooty` (his own tethered-boot tool for this same
  limera1n/A4 hardware family) or his `sn0wbreeze`/`xpwn` forks.
- Directly compared against `firecore/Seas0nPass`'s real `saffron`
  untether payload (`bundles/AppleTV2,1_4.3_8F455.bundle/saffron.tgz`'s
  `./boot/untether`) on the theory that AppleTV's other major
  `dirhelper`-hijacking tool might share code — it doesn't: different
  architecture (armv6 `PREBOUND` vs. our armv7 `PIE`), different size
  (8,192 vs. 50,240 bytes), zero shared strings, and fundamentally
  different content (`saffron`'s is a low-level IOKit/ROP kernel
  re-exploitation payload; ours is a high-level userspace installer).
  Also confirmed Seas0nPass never shipped anything for AppleTV3,x or past
  6.2.1 firmware at all, ruling it out as a source on device/firmware
  grounds alone.
- The *technique* — not the binary — traces cleanly to **evasi0n6**
  (`OpenJailbreak/evasi0n6`, the real archived source of evad3rs' iOS
  6.0–6.1.2 untethered jailbreak, Oct 2012). Their real Cydia package
  (`com.evad3rs.evasi0n`) installs exactly `/usr/libexec/dirhelper`
  (`untether/package.sh`: `cp ../kernel/extras/dirhelper
  package/usr/libexec/dirhelper`), and their `jailbreakinstaller.m`'s
  `prepare_jailbreak_install()` contains
  `run((char *[]) {"/sbin/mount", "-u", "-o", "rw,suid,dev", "/", NULL},
  NULL)` — the exact same mount flags in the exact same order as this
  binary's own `remount()`, built on the same `posix_spawn`+`waitpid`
  `run()`-helper pattern this binary's own imports match, bootstrapping
  Cydia from a bundled archive (`kernel/Cydia.tar.lzma`) the same way
  ours does from `/private/var/Cydia.tar`. But even evasi0n6's own team
  never published *their* dirhelper's source either — their Makefile
  just does `cp dirhelper $(TEMPDIR)/usr/libexec/dirhelper` from a
  pre-built file, and that exact file is now a broken symlink
  (`../../kiki/ddi/dirhelper`, target missing) even in their own archived
  repo.

Best real conclusion: this is almost certainly someone's (plausibly
iH8sn0w's, given the watermark, who was active and cross-connected across
this exact scene) adaptation of evasi0n6's well-documented "hijack
`dirhelper`, remount `rw,suid,dev`, bootstrap Cydia from a bundled
archive" technique to the AppleTV/DFU-restore context — but the compiled
binary itself was never open-sourced by anyone at any point, the same way
the very team that invented the technique never open-sourced theirs. Not
recoverable by more searching; this is the actual, fully-investigated
answer, not a gap in the investigation.

## Loose files recovered from the deleted `ramdisk/` overlay

Everything below was either a top-level file under the old
`Blackb0x/ramdisk/files/` (not part of any of the six tarballs at all —
originally opaque Xcode bundle resources in the original macOS app, or this
fork's own additions) or one of the four still-relevant members extracted
individually from `ATV-Cydia.tgz` before that tarball was deleted. All kept
flat here rather than mirroring their old destination paths, matching this
directory's existing convention — **except** the apt-repo credential/list
files (every `.list`, `.gpg.key`, and standalone `.gpg`/`trustdb.gpg` below),
which live under `apt/` instead, one level deeper than everything else here,
so `scripts/build_deb_cache.py` (see its own `APT_DIR`) has a single
directory to glob rather than filtering repo config out of a flat `Misc/`
listing that also holds unrelated payloads.

- `xyz.regulad.blackb0x.postinstall.plist` — LaunchDaemon plist (see
  NEO_FLOW.md for what installs/runs it). No
  `com.openssh.sshd.plist` here anymore — NEO_FLOW deliberately doesn't
  bootstrap an early sshd; the real `openssh` package's own LaunchDaemon
  plist is the only one that ever starts sshd now, once `postinstall.sh`
  installs it. The original disassembled binary did stage one — see "What
  entrypoint installs" below for that historical record.
- `apt/joshtv.gpg.key` — **renamed from `pubkey.key`**: confirmed via
  `gpg --show-keys` to be JoshTV's own signing key (`uid: JoshTV
  <jtv@JoshTV.net>`) — the old name gave no hint which repo it signed for.
  `joshtv.list` itself (`http://joshtv.net/ATV2/`) is **deleted**: the
  domain now resolves to a parked/ad-redirect page (confirmed via a live
  Wayback Machine snapshot — the only capture the crawler ever made of
  `/ATV2/`, and it's already the parked page, not real repo content), and
  [NSSpiral/Blackb0x#50](https://github.com/NSSpiral/Blackb0x/issues/50) —
  the upstream project's own thread on this — confirms the community found
  no working replacement mirror either, only the unrelated `awkwardtv.list`
  workaround below. `joshtv.gpg.key` is kept for now anyway (not deleted
  yet) even though nothing currently points at it.
- `apt/awkwardtv.list` — `deb https://apt.awkwardtv.org/ stable main`, the
  community's own proven replacement for the dead `joshtv.list` (see above)
  — **not a mirror of JoshTV's content**, a different, still-independently-
  maintained repo that happens to cover the same install-time dependencies
  (`org.awkwardtv.whitelist`, `com.nito.updatebegone`, etc.). Verified live:
  `dists/stable/Release`/`Release.gpg` both return 200 (a real, signed
  Debian-style repo, unlike the bare unsigned `xbmc.list`/old `joshtv.list`
  flat repos).
- `apt/awkwardtv.gpg.key` — AwkwardTV's real signing key (fetched from
  `https://apt.awkwardtv.org/awkwardtv.pub`, `uid: AwkwardTV APT Signing Key
  <apt@awkwardtv.org>`). Confirmed to actually sign this repo, not just
  claimed: verified with a real `gpg --verify` of `dists/stable/Release`
  against `Release.gpg` using this exact key — "Good signature".
- `apt/saurik.list` — `deb http://apt.saurik.com/ ios/8.0 main`, saurik's own
  default Cydia/Telesphoreo repo (this is where `dpkg`, `syslogd`, and most
  of the rest of the base package set in `debcache/` actually came
  from — confirmed, not assumed: `dists/ios/8.0/main/binary-iphoneos-arm/
  Packages` lists `dpkg_1.18.10-12_iphoneos-arm.deb`, byte-identical to what's
  already vendored here). `ios/8.0` (not `ios/6.0`/`ios/7.0`) specifically:
  it's the newest of saurik's per-major-iOS-version distributions and, per
  that same Packages file, still lists all the older packages this project
  needs — checked directly rather than assumed forward-compatible.
- `apt/saurik.gpg.key` — Jay Freeman (saurik)'s real signing key. Not re-sourced
  externally: extracted directly from `trusted.gpg` (recovered from
  `ATV-Cydia.tgz`, see above) via `gpg --export`, since Cydia ships this key
  pre-trusted in its own keyring — this is the device's own already-trusted
  copy, not a fresh download. Confirmed to actually sign this repo with a
  real `gpg --verify` of `dists/ios/8.0/Release` against `Release.gpg` —
  "Good signature", fingerprint `4E1B 2D7E A821 9B7E 045A 7540 6D36 863D
  AA31 C175`.
- `apt/regulad.list` — this fork's own default repo (`https://ios.regulad.xyz/`).
- `apt/regulad.gpg.key` — this fork's own signing key, confirmed via
  `gpg --show-keys` (`uid: Parker Wahle (iOS apt repository)
  <regulad@regulad.xyz>`).
- `apt/xbmc.list` — Kodi's official ATV2 repo mirror.
- `apt/net.tihmstar.list` — `deb http://repo.tihmstar.net/ ./`, tihmstar's
  own beta repo (see `### Direct provenance, found via the "Home Depot"
  lead` above — this is the exact repo `net.tihmstar.etasonuntether`/
  `net.tihmstar.daemonloader`/`net.tihmstar.untetherhomedepot` were pulled
  from directly, and it's still live). Flat, `./`-style layout, exactly the
  `deb` line the repo's own homepage instructs (`repo.tihmstar.net/`'s HTML
  literally says to `echo` this exact line into
  `/etc/apt/sources.list.d/net.tihmstar.list`).
- `apt/net.tihmstar.gpg.key` — tihmstar's real signing key. Not guessed:
  `Release.gpg` is a real detached signature (issuer fingerprint `6A8A C255
  F7E1 43F8 95C2 8427 D036 3332 8074 6A0D`); fetched the matching public key
  from `keyserver.ubuntu.com` by that fingerprint (`uid: tihmstar
  <tihmstar@gmail.com>`, primary fingerprint `3F72 1DB6 4D54 CFA2 AE9F D4B7
  EE5A 76FA 6F6E 76D7`) and confirmed with a real `gpg --verify` of
  `Release` against `Release.gpg` — "Good signature from \"tihmstar
  <tihmstar@gmail.com>\"" (the key itself expired 2018-10-01, well after
  this 2017-10-01 signature was made, so `gpgv`/apt will flag it as expired
  rather than reject it outright — same "genuinely valid, just dated"
  situation as `saurik.gpg.key`/`bigboss.gpg.key`).
- `apt/bigboss.list` — `deb http://apt.thebigboss.org/repofiles/cydia/ stable
  main`, TheBigBoss's own real, live Cydia repo. Verified live, not assumed:
  `dists/stable/Release`/`Release.gpg` both return `200` with real content
  (a genuine signed Debian-style repo). The plain-text
  `dists/stable/main/binary-iphoneos-arm/Packages` itself 302-redirects to
  a `thebigboss.org` sales page regardless of User-Agent (Telesphoreo/Cydia/
  Debian APT UAs all get the same redirect) — looks like an anti-scraping
  rule targeting that one large (~8MB) plain-text file specifically, since
  an unrelated bogus path under the same `dists/stable/` returns a real
  `404`, not the same redirect. Not treated as a dead repo on that basis:
  the same Release's `main/binary-iphoneos-arm/Packages.bz2` (a smaller
  file) returns a real `200` with genuine `bzip2` data, and decompressing
  it gives a file whose SHA256
  (`d2d25a4e08826b39d2bb9aa583eb8624f9e247eb9040213673ac477081527ac7`,
  8245183 bytes) matches `Release`'s own MD5Sum/SHA256 manifest for
  `main/binary-iphoneos-arm/Packages` exactly, byte-for-byte — confirmed
  directly, not assumed — and its contents are genuine current BigBoss
  package stanzas (`abgrouper`, `actionmenu`, etc.), not placeholder/parked
  content. `main/binary-iphoneos-arm64/Packages` also returns a real `200`
  directly.
- `apt/bigboss.gpg.key` — BigBoss's real signing key (`uid: BigBoss
  <bigboss@thebigboss.org>`, DSA-1024, fingerprint `A9C9 6A37 1158 94A2
  3B89 4107 694D 17D3 8764 B4F4`). Not re-sourced externally: extracted
  directly from `trusted.gpg` (recovered from `ATV-Cydia.tgz`, see above)
  via `gpg --export`, the same way `saurik.gpg.key` was — this is the
  device's own already-trusted copy, not a fresh download (re-running the
  same `gpg --export` command against `trusted.gpg` reproduces
  `saurik.gpg.key`/`saurik.gpg` byte-for-byte, confirming the method).
  Confirmed to actually sign this repo with a real `gpgv --keyring` /
  `gpg --verify` of `dists/stable/Release` against `Release.gpg` using this
  exact key — "Good signature from \"BigBoss <bigboss@thebigboss.org>\"",
  signing key ID `694D17D38764B4F4` (matches the issuer key ID in
  `Release.gpg`'s own signature packet). Same 2008-era DSA-1024/SHA-1
  profile as `saurik.gpg.key`/`awkwardtv.gpg.key` — genuinely valid per
  plain `gpgv`, just outside apt's own internal weak-digest policy (see
  `scripts/build_deb_cache.py`'s module docstring).
- `kodi.png`, `nito.png` — Frappliance icons for Kodi/nitoTV. nitoTV itself
  was never wired up in the legacy flow — its install block in `postinstall.sh` is
  entirely commented out, with no explanation beyond a stray "(Credit:
  JoshTV)" — because the original repo never had a live source for it, per
  the user. It does have one now: **`com.nito.nitotv`** (note: lowercase
  `nitotv`, not `nitoTV` — that's the real `Package:` field, confirmed from
  the `.deb`'s own control file) is hosted on `apt.awkwardtv.org`, the same
  repo `awkwardtv.list` above already points at. Added
  `com.nito.nitotv_0.8.7-33_iphoneos-arm.deb` (the current latest),
  `com.nito.tssagent_1.3-18_iphoneos-arm.deb` (a dependency), and
  `com.firecore.freemem-watcher_1.0-2_iphoneos-arm.deb` (another dependency)
  to `debcache/` and `com.nito.nitotv`/`com.nito.tssagent`/
  `com.firecore.freemem-watcher` to `packages.txt` — all downloaded and
  byte-verified against `apt.awkwardtv.org`'s own `Packages` index (MD5
  match), and structurally verified (`ar`/`tar` extract cleanly).
  **Initially pinned to `0.8.5-5`**: every `com.nito.nitotv` release from
  `0.8.6-7` onward adds a bare `Depends: syslogd` that nothing already
  vendored here provided at the time — exactly the "unmet dependencies"
  failure several forum/GitHub reports describe for nitoTV installs from
  this era. **Resolved and upgraded to `0.8.7-33`**: `syslogd` turns out to
  be a real, tiny standalone package (just drops `/etc/syslog.conf` — the
  actual `syslogd` binary is Apple's own, already on the device; this
  package exists purely to satisfy the dependency by name) by saurik
  himself, at `http://apt.saurik.com/debs/syslogd_1.1.1_iphoneos-arm.deb`
  (found by the user). Added `debcache/syslogd_1.1.1_iphoneos-arm.deb`
  and `syslogd` to `packages.txt`, byte-verified against that URL, and
  confirmed present with an identical MD5 in `saurik.list`'s own `Packages`
  index too (see below) — with `syslogd` no longer missing, `0.8.7-33`'s
  full dependency chain (`beigelist>=2.2.5-10`, `mobilesubstrate`,
  `cydia>=1.1.1`, `com.nito.tssagent>=1.3-18`, `com.firecore.freemem-watcher`,
  `syslogd`) is fully satisfied by what's already vendored, so that's what's
  pinned now instead of `0.8.5-5`.
- `profile` — replacement root `.profile` (colors, prompt, `ls` alias).
- `rtbuddyd.bin` — the pre-`jsc`-swap tether-only `rtbuddyd` replacement
  used by `install_blackb0x_tether()`'s branch, in the removed tether flow.
- `postinstall.sh` — the legacy first-boot installer script, being fully rewritten
  per NEO_FLOW.md; kept here as the pre-rewrite reference.
- `apt/trustdb.gpg`, `apt/trusted.gpg` — Cydia's apt keyring database, extracted
  from `ATV-Cydia.tgz` before that tarball was deleted.
- `firmware.sh.old`, `startup.old` — Cydia's own pre-jailbreak firmware/
  startup scripts, also extracted from `ATV-Cydia.tgz` before deletion.

**Not recovered, on purpose**: `.blackb0x` (an empty marker file the
installer drops post-install — zero content, trivially recreatable,
nothing to preserve).

## etasonATV / tihmstar-untether provenance

`Blackb0x/ramdisk/files/etasonATV/` is tihmstar's EtasonATV untether payload
(the first public AppleTV 3 jailbreak, targeting AppleTV3,1/3,2 on firmware
7.4/12H876 and 7.5/12H885), delivered live via a DNS-hijack + Trailers-app
WebKit exploit chain (see https://etasonatv.tihmstar.net/). Blackb0x's
ramdisk-stage `sbin/launchd` installer copies these files onto the target
volume directly, selecting them for its "iOS 8.4 untether" branch — it never
needed tihmstar's live delivery chain, since Blackb0x already has root
filesystem access via the DFU/checkm8 restore-ramdisk path.

We tried to independently verify/re-source this payload against tihmstar's
live infrastructure and failed:

- `46.166.144.59` (the original DNS-hijack IP tihmstar tweeted) is dead —
  no DNS response, ports 80/443 both closed.
- `188.166.193.52` (the current DNS-hijack IP) is alive and still nginx-vhost
  aware for `Host: trailers.apple.com` — it 301s to
  `https://etasonatv.tihmstar.net` instead of serving exploit content
  directly. Without that Host header it falls through to an unrelated
  default vhost (a "doubleH3lix" jailbreak landing page).
- DNS resolution *through* that IP (`dig trailers.apple.com @188.166.193.52`)
  returns a malformed EDNS-only response with no actual answer — the
  DNS-hijack step itself looks non-functional today, even though the HTTP
  layer behind it is still configured.
- The profile certificate path named in the site's own instructions,
  `trailers.apple.com/whyIsThisFileNameSoLong.cer`, returns a 500 on
  `etasonatv.tihmstar.net` directly (an nginx location still exists for it,
  but whatever backend generates the cert is broken) rather than a 404.
- `untether.deb`, `expl.js`, `exploit.js`, `etason.js`, and every other
  filename we guessed all came back clean 404s. Even the site's own embedded
  screenshots (`images/instructions/step1.JPG`, etc.) 404 — the page itself
  admits "a few images are missing."

Conclusion (superseded below): there is no live, independently-downloadable
copy of this payload anywhere via `etasonatv.tihmstar.net`'s own DNS-hijack
delivery chain specifically. It was only ever distributed in-band during a
live exploit run through that one vector — but see below, since tihmstar
also shipped (and still serves) essentially the same payload as a normal
Cydia package.

### Direct provenance, found via the "Home Depot" lead

The `/var/logs/untetherhomedepot.log` /
`/var/logs/untetherhomedepotLoopProtection.txt` strings burned into
`untether/untether.bin` are not a coincidence or a mislabeled file — they
pointed straight at the real answer. tihmstar's own Cydia repo,
**`repo.tihmstar.net`, is still live today** (`nginx/1.24.0`, `Packages.gz`
still serves a real, current index) and hosts exactly the sibling packages
this file's strings reference:

| package | version | source |
|---|---|---|
| `net.tihmstar.etasonuntether` | 1.3.1 | `repo.tihmstar.net/debs/net.tihmstar.etasonuntether-1.3.1.deb` — "Untether for 8.4.1 32bit... bootstraps Etason jailbreak" |
| `net.tihmstar.untetherhomedepot` | 1.4.2 | `repo.tihmstar.net/debs/net.tihmstar.untetherhomedepot.deb` — "Untether for 9.1-9.3.4 32bit... bootstraps HomeDepot jailbreak" |
| `net.tihmstar.daemonloader` | 1.0.0 | `repo.tihmstar.net/debs/net.tihmstar.daemonloader-1.0.0.deb` — the `orphan_commander`/`etc/rc.d/daemonload` mechanism, shared by both untethers per its own package description ("Use this to have openssh loaded with etasonuntether or untetherhomedepot") |

Downloaded all three directly (MD5-verified against the repo's own
`Packages` index) and diffed against what's checked into this repo:

- **`untether/expl.js`, `usr/bin/orphan_commander`, `etc/rc.d/daemonload`
  are byte-identical (`cmp`-verified)** to the files inside
  `net.tihmstar.etasonuntether-1.3.1.deb`, straight from tihmstar's own,
  still-live repo. This is about as direct a provenance confirmation as
  this project has for anything: 3 of the 4 real payload files are
  confirmed straight from the author's own current distribution.
- **`untether/untether.bin` (and its `orig_untether.bin` backup copy) do
  NOT match.** The real `net.tihmstar.etasonuntether-1.3.1.deb`'s
  `untether.bin` is 35,677 bytes (dated 2021-04-04 in that package). The
  real `net.tihmstar.untetherhomedepot.deb`'s `untether.bin` (2017) is
  33,600 bytes. Ours is 43,680 bytes — bigger than both, and its very first
  divergent instruction from either real binary is 4 bytes into the file
  (all three share the same 2-instruction Thumb prologue, `push {r7,lr}; mov
  r7,sp`, then diverge immediately), so it's a genuinely different compiled
  build, not a small patch of either published one.
- Our own `untether.bin` vs. our own `orig_untether.bin` (both 43,680
  bytes) differ in exactly 15 bytes — **corrected**: these are NOT
  conditional-branch immediates (an earlier pass here misread them as
  that). Direct disassembly/diff of both files shows 14 of the 15 bytes
  (file offset `0x9865`-`0x9893`) are an in-place ASCII string edit, not
  code: the patch swaps one of the binary's four embedded `Darwin Kernel
  Version` banner strings from `...xnu-2784.40.6~50/RELEASE_ARM` (Fri Nov
  11 2016) to `...xnu-2784.40.6~93/RELEASE_ARM` (Fri Jan 29 2021) — both
  strings exactly the same length, which is *why* an in-place same-size
  patch was even possible without relinking. The other three banner slots
  (`~86`/Sep 2019, `~87`/Feb 2020, `~92`/Nov 2020 — see the kernel-banner
  table below) are untouched in both files. Net effect: drop the oldest
  recognized kernel build, add the newest known one, keeping the table's
  total size fixed at 4 entries — consistent with hand-editing a compiled
  fixed-size array, not recompiling with a 5th slot. The remaining 1 byte
  (offset `0x3301`) is a real code change, `ADDS r6, #0x28` → `ADDS r6,
  #0x45` (40 → 69 decimal), elsewhere in the binary — not tied to the
  banner table, and what it actually does is still unexplained. This is
  the same "keep an `orig_` backup, ship a patched copy" pattern this
  project itself uses elsewhere (see `spliceFileContentInPlace()` in
  `BakeRamdisk.cpp`). The tar's own internal file timestamps line up with
  this: `orig_untether.bin` is 2021-04-17, `untether.bin` is 2021-04-28 —
  both shortly after tihmstar's own 2021-04-04 `etasonuntether-1.3.1`
  release, and just 8-19 days after Apple's real `12H923` tvOS 8.4.x
  security update (2021-04-09, per theapplewiki.com) — whoever patched
  this was tracking Apple's still-ongoing tvOS 8.4.x point releases in
  close to real time.
- Our `untether.bin`'s embedded kernel-banner table (four `Darwin Kernel
  Version`/`xnu-2784.40.6~NN` strings, all SoC `S5L8947X` — the Apple TV
  3's real SoC) is itself a sharp contrast with the *officially packaged*
  `1.3.1` deb: that build's own table has five entries, all dated the same
  day (Aug 5 2015), all `xnu-2784.40.6~18`, for five *other* SoCs
  (`S5L8940X`/`8942X`/`8945X`/`8950X`/`8955X` — iPhone4S/iPad2/iPad3/
  iPod5/iPhone5-class devices) and **never once mentions `S5L8947X`**,
  despite being nominally the Apple TV 3 untether. That lines up with the
  deb's own control file: `Description: Untether for 8.4.1 32bit`,
  changelogged for "v1.2 adds support for iPhone5", "v1.3 adds support for
  iPad4" — it's a generic multi-device payload, hard-pinned via `Depends:
  firmware (= 8.4.1)`, not something ever actually verified/built against
  a real Apple TV 3 kernel banner. Our `untether.bin`, by contrast, checks
  against several real, sequential tvOS 8.4.x point-release kernels
  specifically. Wayback Machine's crawl of `repo.tihmstar.net` confirms
  only two `etasonuntether` releases ever existed (`1.3.0`, first crawled
  2020-09-16 but internally dated 2017-09-25, and `1.3.1`, 2021-04-04) —
  and their `untether.bin` files are byte-identical to each other
  (`sha256` match), so there was never a third, official release matching
  our 43,680-byte build either.
- `strings` on `untether.bin` reads like a classic 32-bit kernel-exploit
  harness — `patching cs_enforcement_disable_amfi`, `patching
  amfi_substrate`, `patching proc_enforce`, a literal `"Marijuan"` 8-byte
  watermark string written over the kernel's own `RELEASE_ARM` osrelease
  string. **That exact function set and that exact `"Marijuan"` watermark
  also appear, as real C source, in tihmstar's own public GitHub repo**
  ([`tihmstar/jelbrekTime`](https://github.com/tihmstar/jelbrekTime), the
  `jelbrekT1m3 WatchKit Extension/jailbreak.m` file — a *different* jailbreak
  entirely, for watchOS 4.1 on the Apple Watch Series 3):
  ```c
  //  jailbreak.m
  //  v0rtex
  //  Created by tihmstar on 14.12.17.
  ...
  uint32_t marijuanoff = (uint32_t)memmem(kdata+i_can_has_debugger_dst-kernel_base, ksize, "RELEASE_ARM", sizeof("RELEASE_ARM")-1)-kdata;
  kern_return_t kr=vm_write(taskHacked, marijuanoff+kernel_base, "Marijuan", 8);
  ```
  This is tihmstar's own reusable kernel-jailbreak harness, internally
  codenamed **"v0rtex"** per that file's own header comment — one generic
  C source he's compiled into at least three different deliverables over
  the years: EtasonUntether (AppleTV/iOS 8.4.1), UntetherHomeDepot (iOS
  9.1-9.3.4), and jelbrekTime (watchOS 4.1, the one whose source he
  actually published). The watermark isn't a one-off either — it's a
  standing signature technique across tihmstar's whole toolchain: his
  real, currently-maintained [`tihmstar/libpatchfinder`](https://github.com/tihmstar/libpatchfinder)
  has a dedicated, reusable `get_MarijuanARM_patch()` used for both
  32-bit and 64-bit targets, including a 32-bit iOS 8 kernel-patchfinder
  file (`kernelpatchfinder32_iOS8.cpp`) whose own header comment reads
  "Created by tihmstar on 13.08.21" — about 3.5 months after this
  tarball's own timestamps. Circumstantial, not proof, but consistent with
  our `untether.bin` being a snapshot of tihmstar's own private,
  actively-developed 2021-era tooling that was never packaged into a
  public `.deb`, rather than a third party's build.

**Conclusion:** this repo's `tihmstar-untether.tar` is real, author-sourced
material, not a mystery artifact — `expl.js`/`orphan_commander`/`daemonload`
are confirmed byte-identical to tihmstar's own still-live Cydia package, and
`untether.bin` is a hand-patched build of his own publicly-acknowledged
"v0rtex" kernel-harness lineage (source partially public via `jelbrekTime`,
even though this exact compiled variant isn't). The 15-byte patch itself is
now fully characterized (see above: 14 bytes are a same-length kernel-banner
string swap, 1 byte is an unexplained code-immediate tweak elsewhere), and
an intermediate-official-release hypothesis for the base build is ruled out
(Wayback Machine shows only two `etasonuntether` releases ever existed,
`1.3.0`/`1.3.1`, with byte-identical `untether.bin`s). What's still
genuinely open: the actual origin of the *unpatched* `orig_untether.bin`
base build itself — bigger than, and only ~33-35% opcode-similar to, either
currently-published `etasonuntether`/`untetherhomedepot` release, so not a
recompile of either — and who applied the 15-byte patch (no name/signature/
commit trail recovered anywhere). See `.claude/TODO.md`.

### Full snapshot of `repo.tihmstar.net`, archived while it's still up

Once `repo.tihmstar.net` turned out to still be live (see above), we pulled
every package it currently lists, not just the four this project actually
uses — since finding it alive at all this many years on was already a
surprise, there's no telling how long that stays true. All 35 entries in
its `Packages`/`Packages.gz` index returned a real `200` (nothing 40x'd),
and every one MD5- and size-verified against the index's own manifest
before being added to `debcache/`. Beyond the four already documented
above (`net.tihmstar.etasonuntether-1.3.1.deb`,
`net.tihmstar.daemonloader-1.0.0.deb`, `net.tihmstar.untetherhomedepot.deb`,
`net.tihmstar.untetherhomedepotoffsets.deb`), this pulled in tihmstar's
other exploit-scene tooling (`kloader-1.0.0.deb`, `libcrippy-1.0.0.deb`,
`libpartialzip-1.0.0.deb`), a handful of third-party tweaks hosted on the
same repo (`com.56klabs.fuzzyduck`/`fuzzypie`, `com.compilingentropy.
fuzzycactus`, `com.nexuist.webzzuf`, `com.uroboro.tools`), and a long tail
of tihmstar's own small `org.tihmstar.*` jailbreak-detection-bypass/tweak
packages (`NoJBDetect`, `hideMyBadge`, `CGFix`, `secretLauncher`,
`sneakySelfie`, etc. — mostly unrelated to this project's own jailbreak
chain, kept purely for archival reasons since the repo itself might not
stay up).

**None of this is wired into `packages.txt`** — these are a pure archival
snapshot, not something `bake-firmware` installs. Curating which (if
any) are worth actually shipping is a separate decision from preserving
them before the repo disappears.

### The `jsc`/`rtbuddyd`/`--early-boot` mechanism, resolved

`entrypoint/`'s reverse-engineering (see that directory's own README) turned
up two things the disassembly alone couldn't explain: a symlink aliasing
`jsc` (JavaScriptCore's CLI interpreter) as `rtbuddyd`, and a second symlink
from `expl.js` to a file literally named `/--early-boot`. Resolved via
tihmstar's own conference talk on this exact exploit chain ("Jailbreaking
the AppleTV 3 — Tales from a full stack hack", see the link in
`AGENTS.md`'s External references):

- `rtbuddyd` is a normal Apple daemon launchd auto-starts at early boot,
  invoked as `rtbuddyd --early-boot`.
- The untether replaces the on-disk `rtbuddyd` binary with Apple's own
  `jsc` — still validly signed, so it passes code-signing checks that would
  block an unsigned or patched binary outright.
- `jsc` treats its argument as a script path to open and execute. So
  launchd's `rtbuddyd --early-boot` invocation actually runs `jsc
  --early-boot`, and `jsc` resolves that as the file `/--early-boot`,
  follows the symlink to `expl.js`, and executes it as JavaScript.

There's no OS-level boot-arg mechanism involved at all — `/--early-boot` is
just the accidental file path that falls out of `jsc`'s own argv-handling
once it's substituted in for a daemon that's always started with that
particular flag. `expl.js` is the WebKit/JSC exploit itself, so this
sequence re-establishes kernel R/W and re-applies the kernel patches fresh
on every single boot — the entire untether, with zero genuine kernel/launchd
support for persistence, just a clever abuse of one daemon's calling
convention.

## `sbin/launchd` — source and pinned paths

`Blackb0x/ramdisk/sbin/launchd` is not Apple's launchd — it's a precompiled
ARMv6 Mach-O that replaces PID 1 on the ramdisk just long enough to lay the
jailbreak payload onto the real filesystem, then hands off to the real
launchd. Its own strings self-identify it: `blackb0x Jailbreak - by
@NSSpiral`. No source for it exists anywhere we could find — not in
NSSpiral/Blackb0x itself (bundled only as an opaque Xcode resource, loaded
via `[[NSBundle mainBundle] pathForResource:@"launchd" ofType:@""]` in the
original `Patcher.mm`), not in any of its ~30 GitHub forks, not in NSSpiral's
other public repos, and it doesn't turn up in a GitHub-wide code search for
its own distinctive strings (`"Installing etasonATV"`,
`"Installing blackb0x tether"`, `"Cloning directory"`). It was most likely
hand-built once and never had its own build recipe committed anywhere.

Everything below was recovered by disassembling its embedded strings
(`strings Blackb0x/ramdisk/sbin/launchd`) — this is the ground truth for
what the binary reads off the ramdisk's `/files/` tree at boot, independent
of anything `postinstall.sh` or the new C++ code does later:

- Loose top-level files: `.blackb0x`, `com.blackb0x.postinstall.plist`,
  `com.openssh.sshd.plist`, `fstab.atv` (gutted — its two lines only
  duplicated the entrypoint's own hardcoded `mount()` calls for
  `/dev/disk0s1s1`/`/dev/disk0s1s2`, and it was only ever installed by the
  6.1.4/`p0sixspwn` branch — nothing left worth keeping), `joshtv.list`,
  `kodi.png`, `nito.png`, `profile`, `pubkey.key`, `rtbuddyd.bin`,
  `postinstall.sh`, `xbmc.list`.
- `files/cydia/` — walked recursively (`bin/`, `sbin/`, `usr/{bin,sbin,lib,
  libexec,include,share}/`, plus a long list of individual pinned files:
  `usr/libexec/{ssh-keysign,sshd-keygen-wrapper}`, `usr/include/db{,_cxx}.h`,
  `usr/lib/apr.exp`, `usr/lib/dpkg/methods/apt/{desc.apt,names}`,
  `usr/lib/libintl.{8.0.2.dylib,la}`, `private/etc/alternatives/README`,
  `private/etc/profile.d/{coreutils,less}.sh`, `private/var/run/utmp`,
  `System/Library/LaunchDaemons/com.saurik.Cydia.Startup.plist`,
  `usr/share/{gnupg/options.skel,dpkg/,keyrings/,Ssh.bin,tabset/,
  bigboss/icons/}`, and the full `usr/share/terminfo/` set).
- `files/p0sixspwn/` — `private/etc/launchd.conf`,
  `private/var/lib/dpkg/info/com.ih8sn0w-squiffy-winocm.p0sixspwn.{list,
  postinst,prerm}`, `private/var/untether/{_.dylib,untether}`,
  `usr/libexec/dirhelper`.
- `files/etasonATV/` — `etc/rc.d/daemonload`, `usr/bin/orphan_commander`,
  `untether/{untether.bin,expl.js}`.
- 8 pinned `.deb` filenames (see below).

### Deb filenames are pinned to exact old revisions

The binary does an exact filename match before it will move a `.deb` into
place — it doesn't glob or fuzzy-match. Comparing its 8 pinned filenames
against what's actually in `debcache/` today:

| pinned by `sbin/launchd` | currently in `debcache/` | same content? |
|---|---|---|
| `beigelist_2.2.6-30_iphoneos-arm.deb` | same | yes, exact match |
| `com.nito.updatebegone_0.2-1_iphoneos-arm.deb` | same | yes, exact match |
| `com.saurik.patcyh_1.2.0_iphoneos-arm-fixed.deb` | `com.saurik.patcyh_1.2.0-1_iphoneos-arm.deb` | no — cached copy is control-file version `1.2.0-1`, one revision newer |
| `ldid_1-1.2.1_iphoneos-arm.deb` | `ldid_1.2.1_iphoneos-arm.deb` | filename only — cached copy's real control-file `Version:` is `1:1.2.1` (epoch 1); the pinned name correctly encodes that epoch as `1-1.2.1`, our renamed copy dropped it |
| `rtadvd_307.0.1-2_iphoneos-arm-fixed.deb` | `rtadvd_307.0.1-3_iphoneos-arm.deb` | no — cached copy is `307.0.1-3`, one revision newer |
| `sqlite3-dylib_3.5.9-1_iphoneos-arm-fixed.deb` | `sqlite3-dylib_3.5.9-2_iphoneos-arm.deb` | no — cached copy is `3.5.9-2`, one revision newer |
| `sqlite3-lib_3.5.9-2_iphoneos-arm-fixed.deb` | `sqlite3-lib_3.5.9-3_iphoneos-arm.deb` | no — cached copy is `3.5.9-3`, one revision newer |
| `uikittools_1.1.12_iphoneos-arm-fixed.deb` | `uikittools_1.1.12-1_iphoneos-arm.deb` | no — cached copy is `1.1.12-1`, one revision newer |

Only 2 of 8 still match exactly. The other 6 will silently fail to be found
by this binary at boot unless a copy is placed under the old pinned name too
(see the open item in the top-level project punch list).

### Which deb this reveals we still need

This binary force-installs `sqlite3-dylib` and `sqlite3-lib` directly by
name — it doesn't rely on apt pulling them in as a dependency of something
else. `sqlite3-dylib` is fine either way (it's still a real dependency of
another root package, so it'd get pulled in regardless). **`sqlite3-lib` is
not** — nothing in this project's dependency graph formally depends on it,
which is exactly why an earlier graph-based trim dropped it from what was
then `persist_packages.txt` (since merged with the former
`ramdisk_packages.txt` into `package/packages.txt`). But this binary
explicitly installing it by name, before dpkg dependency resolution is even
in the picture, means it's still a load-bearing package, not dead weight
the graph missed correctly — it's back in `packages.txt` now, but the
graph-based approach alone would have dropped it again on "no known
dependent" grounds if this hadn't been caught here first.
